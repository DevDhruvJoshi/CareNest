NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_ENTERPRISE_URL=http://localhost:5000
# Read-only token for listing camera events from API
NEXT_PUBLIC_READ_TOKEN=dev-read-token

# Optional: If your API is behind a path prefix or proxy, set full URLs
# NEXT_PUBLIC_API_URL=https://api.example.com
# NEXT_PUBLIC_ENTERPRISE_URL=https://edge.example.com

